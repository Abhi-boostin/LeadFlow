enable-pre-post-scripts=true
strict-peer-dependencies=false
auto-install-peers=true
# pnpm 10 skips devDependencies when NODE_ENV=production. Our build pipeline
# needs devDeps (prisma CLI, typescript, tailwind, postcss) at install/build
# time even in production deploys. This override keeps them installed.
prod=false
